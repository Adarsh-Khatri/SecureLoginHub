import styled from 'styled-components';
import { WRAPPER } from './styled.js';


export const WRAPPER_REGISTER = styled(WRAPPER)`

  form{
    
    input[type='file'] {
      display: none;
    }

  }
`;